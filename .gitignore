# Data & artifacts (large, regenerated by scripts).
# Leading slash anchors to the repo root so this does NOT match the ser/data/ package.
/data/
/outputs/
/runs/

# Workflow / scratch artifacts
.audit_workflow.mjs

# Intermediate report HTML (the PDF is the deliverable, kept in git)
docs/RAPOR.html
odev1/RAPOR.html

# Ödev 1 large/regenerable artifacts (keep code, report, figures)
odev1/cache/
odev1/outputs/

# Submission package (zips/bundles for Drive upload — derived artifacts)
submission/
*.ckpt
*.pt
*.pth
*.joblib

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
